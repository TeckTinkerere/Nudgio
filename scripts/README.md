# scripts/

Build, schema, checksum and release tooling (MR-07 repository layout,
MR-20 release pipeline).

Empty for now. `npm run android`, `npm run typecheck`, `npm run lint` and
`npm test` (see the root `package.json`) cover this stage of the project;
release-signing and checksum scripts arrive with the MR-20 release slice.
