# TODO

Actionable, ordered by priority. Details and rationale live in
`docs/KNOWN_ISSUES.md` and `docs/FUTURE_IMPROVEMENTS.md` — this is the
checklist form.

## Before any real-device testing

- [x] Gradle wrapper (`gradlew`/`gradlew.bat`/`gradle-wrapper.jar`) — was missing from the repo entirely, fetched and confirmed it bootstraps (`./gradlew --version` works).
- [x] `@react-native-community/cli` devDependency — was missing, added; `react-native.config.js`'s now-invalid `platforms: { ios: null }` removed for the new CLI's schema.
- [x] Get a local Android SDK + JDK 17 set up and run `cd android && ./gradlew assembleDebug test` — **succeeds as of 2026-08-07**, first time in this project's history. `app-debug.apk` exists on disk; all 70 Kotlin unit tests pass. Took 4 fixes to get there: `compileSdk 37`->`36` (DL-041), AGP `8.12.0`->`8.10.0` for IDE compatibility (DL-044), the TurboModule bridge contract redesign (DL-045), 5 real Kotlin bugs the first compile surfaced (DL-046), a missing `autolinkLibrariesWithApp()` call (DL-047), and one test-authoring bug (DL-048).
- [x] **App now actually launches and renders on a physical device (V2446, Android 16 / API 36) — first time, 2026-08-08.** Took three fixes, all launch blockers, none of which `npm run verify` or `./gradlew test` could have caught: `SoLoader.init(this, false)` -> `OpenSourceMergedSoMapping` (DL-049, native `UnsatisfiedLinkError` before any JS), a missing `src/debug/` source set so the debug build could reach Metro at all (DL-050), and a module-scope `new Intl.PluralRules('en')` that Hermes does not implement, which nulled the whole `features/library` barrel (DL-051). Today + Library tabs verified rendering with no JS errors.
- [x] `./gradlew lintDebug`'s 5 hard-failing errors (DL-068, 2026-08-10) — `NotificationCoordinator.kt`'s three unguarded `manager.notify()` calls now route through a single permission-checked `postNotification()` helper; `AlarmActivity.kt`'s `onBackPressed()` migrated to `OnBackPressedDispatcher`. Not yet re-run against a real `lintDebug` invocation on this machine (Gradle cache contention with a concurrent session — see below); verified by inspection and via an isolated-cache `test`/`lintDebug`/`assembleDebug` run, pending final confirmation.
- [ ] Two new **warnings** (not errors, do not fail the build) from the media-import slice: `UsableSpace` on `MediaStorage.kt:51` and `MediaImporter.kt:71` — lint suggests `StorageManager#getAllocatableBytes`/`allocateBytes` (API 26+, matches minSdk) over `File.usableSpace`/`totalSpace`, since it also considers space the OS could reclaim from other apps' clearable cache. Not wrong today, just conservative; `allocateBytes` is an async, occasionally UI-prompting API and deserves its own pass rather than a rushed swap.
- [ ] Verify `ensureChannels()` fix on a real device/emulator: fresh install, create a Gentle-profile reminder, let it fire without ever tapping "Test reminder" first — confirm the notification appears. (Fixed in `NotificationCoordinator.kt`, now compiles — never yet run on a device.)
- [ ] Verify `commitImport` mode validation on a real device/emulator: send a request with a missing/garbage `mode` from the JS side and confirm it now rejects with `MR_VALIDATION_FAILED` instead of silently merging. (Fixed in `MediaReminderModule.kt`, now compiles — never yet run on a device.)

## Bugs (see `docs/KNOWN_ISSUES.md` for full detail)

- [x] Bottom safe-area insets (DL-052, 2026-08-08). `Screen`'s non-scrollable branch applied no bottom inset at all despite the header claiming otherwise, so fixed-layout and list screens put content and buttons under the gesture bar; `SettingsScreen` meanwhile reserved it twice and showed a dead gap above the tab bar. Fixed in `Screen.tsx` plus a `screenLayout` on `TabNavigator` that zeroes the inset for tab screens, with no per-screen edits. Verified on device.
- [x] Audited every `hasAppBar` usage across `src/features` (2026-08-10) — all 11 screens using it render a real `AppBar` (directly or, for `MediaDetailScreen`, via the `MediaDetailContent` it wraps); `LibraryScreen` deliberately omits it with a comment explaining why. No top-inset bug found.

- [x] Due notification body duplicating the title (DL-069, 2026-08-10) — now shows the linked media's title, falling back to the plain-language schedule summary.
- [x] JVM unit tests for the backup module's pure-Kotlin pieces (`BackupZipStructuralValidator`, `BackupChecksums`, `BackupFormat`, `BackupManifest`, `BackupScheduleRuleCodec`, `BackupConflictPlanner`) — ~40 cases added 2026-08-07, `android/app/src/test/java/com/aslam/mediareminder/backup/`.
- [ ] Add instrumentation tests (`android/app/src/androidTest/`, using the already-declared `androidx.test`/`espresso-core`/`room-testing` deps — no new dependency needed) for `SchedulerCoordinator`, `AlarmActionProcessor`, `AlarmDispatchReceiver`, `AlarmRingingService`, and `BackupExporter`/`BackupImporter` — all Room/Context-dependent, can't run as plain JVM tests.
- [x] Stand up `scripts/release`: version stamping (`stamp-version.js`) and checksum generation (`checksums.js`) added and verified 2026-08-07 — both are plain Node, no Gradle dependency, tested directly. Env-var/`keystore.properties`-based signing wired into `android/app/build.gradle`. `versionCode`/`versionName` now read from `android/version.properties` instead of being hardcoded. See `scripts/README.md`.
- [ ] Run one real `release` build with `minifyEnabled true` and smoke-test it — ProGuard/R8 behavior against the reflection-registered TurboModule and Room has never been confirmed. **No longer blocked**: the `:app:preBuild` Codegen failure this item cited was fixed by DL-045/047, and `assembleDebug` has been green since 2026-08-07.
- [x] Verified `INTERNET` is absent from the **merged release manifest** (2026-08-08). `./gradlew :app:processReleaseManifest` then read `android/app/build/intermediates/merged_manifests/release/processReleaseManifest/AndroidManifest.xml`: exactly 8 permissions (`POST_NOTIFICATIONS`, `SCHEDULE_EXACT_ALARM`, `RECEIVE_BOOT_COMPLETED`, `VIBRATE`, `WAKE_LOCK`, `USE_FULL_SCREEN_INTENT`, `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_MEDIA_PLAYBACK`), no `INTERNET`, and no `usesCleartextTraffic`/`networkSecurityConfig` — DL-050's debug-only additions do not leak into release. The single "INTERNET" string match is the header comment's own MR-06 exclusion list.
- [ ] Turn that release-manifest check into an automated CI gate (MR-18 "repository rules prevent prohibited permissions"). It is currently a manual command whose result is only recorded in this file, so it cannot fail a build.

## Features (see `docs/FUTURE_IMPROVEMENTS.md` for full detail)

- [x] Media import (native + JS) — 2026-08-08. `pickDocument` (Photo Picker for image/video on API 33+, SAF otherwise, via a new `ActivityEventListener` on the module — `MediaPicker.kt`) and `beginMediaImport` (streamed copy + inline SHA-256 + `MediaProbe` metadata + `operation_journal` phases `MEDIA_COPYING`/`MEDIA_PROBING`, `MediaImporter.kt`) are both real. Progress streams via the shared `OperationProgressEmitter` (extracted from the export-only `BackupOperationEmitter`) tagged `kind: "import"`; cancellation shares the extracted `OperationRegistry`. JS: `MediaReminderClient`/`MediaRepository` expose `pickDocument`/`beginImport`/`cancelOperation`; `useImportMedia` (`src/hooks/`, used by both Today and Library since it's not Library-specific) chains pick-then-import, and the "Import media" buttons on both screens now call it for real, with a `ProgressBar` (reusing the `library.import.*` copy that was already seeded in `en.ts` but never wired to anything) and a `Dialog` on failure. 36 new Kotlin unit tests (`MediaKindsTest`, `MediaPickerTest`, `MediaStorageCompanionTest`, plus the earlier `MediaQuerySqlTest`), `./gradlew test`/`assembleDebug` green, `npm run verify` green (44/44, unchanged count — this slice added no new JS tests, see the residual-risk note below). **Verified on a real device the same day** (V2446, Android 16/API 36): tapped Import media, the real system Photo Picker opened (confirmed via `media_grants` SQL in logcat, not just a screenshot), picked a real video, and `beginMediaImport` completed in 451ms (`bridge.call ok:true`) — the item then appeared in the Library grid, sorted "Recently added" first, titled from the file's own name, with no thumbnail (expected — see below). The demo module (`demoNativeModule.ts`) was also extended to simulate the same flow for Metro-only screen dev, but that path specifically was not exercised, since the connected device had the real Kotlin module registered. Still outstanding:
  - [ ] `deleteMedia`, and the delete dependency policy for media a reminder still references. (`updateMedia`/rename landed 2026-08-08, DL-055 — see below.)
  - [ ] Add `reminders.media_id` -> `media_assets` foreign key in its own migration (SQLite cannot add a constraint via ALTER, so `reminders` must be recreated — deliberately not done in `MIGRATION_3_4`, which introduced the parent table).
  - [ ] Thumbnail generation (MR-05 import step 8) — `phase: "creating_preview"` is defined end to end (Kotlin constant, JS phase-label mapping, `en.ts` copy) but never emitted; every import currently goes `copying` -> `checking` -> `ready`.
  - [ ] JS-side test coverage for `useImportMedia`/`importErrorCopy`/`importProgressFraction` — landed with zero new Jest tests, unlike the Kotlin half. Worth a `useImportMedia.test.ts` using `installMockNativeModule`.
  - [ ] Kind-specific import entry points (e.g. "Import audio") — the one "Import media" button defaults to `['image/*', 'video/*']` since MR-05 requires SAF (not the Photo Picker) for audio; documented as a deliberate v1 narrowing in `useImportMedia.ts`, not an oversight.
- [x] Reminder creation, media rename and the MR-03 "Add" FAB (DL-055, 2026-08-08). Reported as "I can upload media but can't access it to set alarms with it" — the real root cause was four separate gaps, all closed: (1) `ReminderEditorScreen`'s media picker read `mockMedia`, not the real library; (2) there was **no way to create a new reminder at all** anywhere in the app — every existing navigation to the editor passed an existing `reminderId`; (3) `MediaDetailScreen` was entirely `findMockMedia`-backed, so opening any real imported item showed "not found" unconditionally; (4) `updateMedia` (rename) was native-unimplemented and `getMedia` (real since DL-053) was never exposed through the JS client/repository layers. Native `MediaRename.resolve()` is a pure, unit-tested function separating the optional-field trim/validate/clamp rules from the Room read-modify-write, matching `MediaQuerySql`'s precedent. `RenameMediaDialog`/`MediaPickerSheet` are their own components, not inlined — both parent screens' own branching already carried enough complexity. The FAB is mounted once in `TabNavigator`, not duplicated per tab, so its `useImportMedia()` progress/error state has one source of truth regardless of which tab is focused. `./gradlew test` green (113 Kotlin tests, 8 new), `assembleDebug` green, `npm run verify` green (44/44, no new JS tests — same residual-risk shape as the import slice). **Verified on device 2026-08-08**: FAB clears the tab bar (compact/bar width only — rail/medium-width still unverified, see below); Add sheet, Create reminder, real media picker (showing genuinely imported items, not fixtures), media selection, label entry, Save, and the new reminder appearing in the Reminders tab all confirmed end to end; rename dialog confirmed (title updates everywhere — header, card, persisted); "Add reminder" from `MediaDetailScreen` confirmed pre-filling the selected item into "What". This pass also surfaced and fixed DL-056, a separate native bug that was silently failing every save until fixed.
  - [ ] Look at the FAB on a real device once unlocked: confirm the 88 dp fixed bottom offset actually clears `AppTabBar` with no gap or overlap on the rail (medium/expanded width) treatment — only the bar treatment (compact phone width) has been checked so far.
- [x] `ReminderDetailScreen`/`ReminderEditorScreen` edit path were still `findMockReminder`-backed (DL-060, 2026-08-09). Both now use a new `useReminderDetail` hook (real `getReminder`); the editor gates on pending/error before the form mounts (fields seed once from `useState`'s initializer, so async data arriving after mount would never reach them). Also fixed while in this file: `ReminderDetailScreen`'s enable `Toggle` was local-`useState`-only and its Delete confirm never called the native side — both now wired to real `setReminderEnabled`/new `useDeleteReminder`.
- [x] `scheduler_state` singleton row was never seeded anywhere, so every reminder save crashed post-commit with `IllegalArgumentException: scheduler_state row must exist` (DL-056, 2026-08-08, found during DL-055's on-device verification). Fixed via `SchedulerStateDao.seedIfAbsent()` called from `SchedulerCoordinator.applyToAlarmManager` before `markDesired`. `./gradlew test`/`assembleDebug` green; verified on device — Save now succeeds with no error, and previously-"failed" saves from before the fix (which had actually silently persisted to Room despite the crash) now schedule correctly on the next reconcile.
- [ ] Full Material 3 visual redesign, in progress (DL-057, started 2026-08-08). Foundation done: `react-native-reanimated`/`react-native-worklets`/`react-native-video` added (release-manifest permission check re-verified green — `react-native-video`'s ExoPlayer dependency injected `ACCESS_NETWORK_STATE`, stripped via `tools:node="remove"`); real native thumbnail generation (`MediaThumbnailer.kt` — actual video frame/downscaled image/embedded audio art, WebP in `cacheDir`, `MIGRATION_4_5`); opaque `thumbnailToken`/`sourceToken` wire contract (`mediaTokens.ts` is the one "image provider" adapter, per MR-08); `MediaPreviewPlayer` in-place playback; `MediaCard` gained a real play affordance + broken-thumbnail-at-render-time fallback; `MediaDetailScreen` split into `MediaDetailContent` + a thin wrapper so `LibraryScreen` can embed it as a real two-pane layout at medium/expanded width (`specs/Markdown/04_Visual_Design_System.md`'s "Medium: ... two-pane Library detail", not previously implemented). `./gradlew test`/`assembleDebug` and `npm run verify` all green. **Not yet verified on a physical device** — reconnect and check: real thumbnails render in the Library grid and Media Detail hero, the play button opens `MediaPreviewPlayer` and actually plays, and the two-pane layout looks right at a tablet/landscape width (emulator or `adb shell wm size`).
  - [x] Home/Today — reanimated `FadeInUp` stagger on occurrence rows, tinted hero avatar for the next reminder (real thumbnail when available, kind icon otherwise). Occurrence timeline data stays mock (pre-existing, separately tracked); visual treatment only.
  - [x] Reminders list + `ReminderDtoWriter.kt`'s hardcoded `mediaKind: "video"`/absent `thumbnailToken` (DL-058, 2026-08-08) — `ReminderMutationService` now joins `media_assets` (batched for `list()`) via a new `MediaDao.getByIds`; row tap navigates to `ReminderDetailScreen`; the enable `Toggle` is wired to a new `useSetReminderEnabled` mutation (previously `onValueChange={() => undefined}` — a decorative row, not a functional one). Leading thumbnail/kind-icon avatar added to each row.
  - [x] `Card` (design-system) now uses the new `AnimatedPressable` internally for a subtle press-scale — one change gives Today/Reminders/Media Detail/Settings the same tactile feedback for free (DL-058).
  - [x] Reminder Details — hero media avatar (real thumbnail or kind icon) and a `FadeInUp` entrance; data source stays `findMockReminder` (separate, already-tracked gap, not touched here).
  - [ ] Remaining screens for the same redesign pass, not yet started: Create Reminder (functional already; animation/visual polish), Settings/Backup/Import (lighter-touch polish passes, already functionally solid), Statistics (richer non-chart visualization — MR-04 is explicitly anti-chart, stays that way), About (tappable rows, typography polish).
  - [ ] `AppTabBar`'s rail treatment at medium/expanded width has never been visually checked on a real device (same gap DL-055 already flagged for the FAB) — worth doing in the same on-device pass as the two-pane Library check above, since both need the same window size.
  - [ ] None of DL-057/DL-058's screens (Library two-pane, Media Detail, Today, Reminders, Reminder Details) have been visually confirmed on a physical device yet — the device disconnected mid-session; every change so far is behind a compile-time check, a unit test, or `npm run verify`/`gradlew test`+`assembleDebug`, not an on-screen look.
- [x] Library selection mode, dedicated Edit Media Asset screen, and app-wide save/delete toasts (DL-061, 2026-08-09). Multi-select with per-card checkboxes, a literal black Back button + Export/Delete swapped in for the normal title/Select row, bulk export (reusing the OS share-sheet `exportMediaAssets` capability) and bulk delete, with haptic+toast feedback for the empty-selection, success, and failure cases. `RenameMediaDialog` replaced outright by a full pushed `EditMediaAssetScreen`, reachable via a new Edit icon beside the title on Media Detail. Reminder create/update/delete and media import now show a top-of-screen success toast (`useSaveReminder`/`useDeleteReminder`/`useImportMedia`), wired once inside each mutation hook rather than at every call site. `npx tsc --noEmit`, full-project `eslint --max-warnings=0`, and `npm test` (44/44) all green. **Not yet verified on a physical device** — same no-device-connected gap as DL-057/DL-058.
- [x] Reminder editor's "choose media" dropdown replaced with a dedicated `SelectMediaScreen` (DL-063, 2026-08-09) — full Library-style thumbnail grid with search/kind filters; tapping a card opens `MediaSelectionPreviewModal` (plays video/audio via `MediaPreviewPlayer`, shows full-res images, icon-only for text) with a persistent "Use this" button; confirming merges the picked id back into the editor's route params and returns automatically (`navigation.navigate(..., {merge: true})`). `MediaPickerSheet` deleted. Known carried-over limitation: the editor still resolves the picked id via a capped 200-item `useMediaList` lookup, not a by-id fetch — see DL-063 for why this wasn't fixed here.
- [x] Fixed a real `MainActivity` crash (`IllegalStateException: Screen fragments should never be restored`) that surfaced during on-device alarm testing (DL-062, 2026-08-09) — `MainActivity.onCreate()` now passes `null` to `super.onCreate()` per `react-native-screens`' documented Android setup requirement. Also diagnosed (not a code bug): `POST_NOTIFICATIONS` was denied on the test device, which independently blocks any notification/full-screen-intent presentation regardless of this fix — user needs to grant it via Settings.
- [x] "Today" renamed to "Upcoming" and rebuilt as a 5-day (today + 4) chronological occurrence view (DL-065, 2026-08-09) — new `projectUpcomingOccurrences.ts` (pure, unit-tested, 15 cases) derives the display list from each reminder's own `schedule` rule (newly exposed on the list DTO, `ReminderDtoWriter.writeSummary`); Next-reminder card now picks the earliest occurrence across the whole window; rows dropped the redundant per-item "Upcoming" pill and gained a play-preview button (reuses the existing `MediaPreviewPlayer`, not a new inline icon-swap control — a disclosed scope trade-off). Internal route key (`tabRoutes.today`), all `testIds.today.*`, and the "TODAY" section heading text are unchanged by design. `npx tsc --noEmit`, full-project `eslint --max-warnings=0`, `npm test` (61/61), and `./gradlew compileDebugKotlin`/`testDebugUnitTest` all green. **Not yet verified on a physical device** — wireless ADB dropped before `installDebug` could push this build.
  - [ ] Explicitly deferred, not built: the per-alarm "Play in Silent / Do Not Disturb" setting and its full permission-state machine (new schema field + editor UI + native `NotificationManager` policy-access checks + `AlarmRingingService` changes) — large enough, and safety-sensitive enough, to warrant its own focused pass.
  - [ ] No continuous midnight-rollover timer while the app sits open on Upcoming — the list refreshes on screen focus (`useFocusEffect`) instead, per the feature's own "avoid unnecessary continuous timers" instruction. Revisit if a user leaves the app open on this tab across midnight and expects it to flip over live.
- [ ] Commit `android/app/schemas/` output as a reviewed artifact and consider a CI check that fails when an entity changes without a matching migration (DL-053 wired `room.schemaLocation`; the schemas are only useful if they are kept and diffed).
- [ ] Backup-import document picker — `pickDocument` (built for media import, above) is a generic primitive `ImportScreen` could reuse to let the user choose a `.zip` archive, unblocking the already-real `inspectBackup`/`commitImport`. Not wired up in this pass.
- [ ] User-defined reminder profiles (`saveProfile`/`resetBuiltInProfile`).
- [ ] `openCapabilitySettings` deep-links from Settings/Health.
- [ ] Onboarding pages 2-3.
- [ ] Health screen per-item capability rows + Test reminder wiring.
- [ ] Full-screen alarm consent / explicit Limited-mode opt-in UI.

- [x] Media Detail "Export this item" and Backup "Share" buttons were dead (`onPress={() => undefined}`) (DL-070, 2026-08-10) — Export now reuses the existing `useExportMedia` mutation for a single item; Share now calls a new native `shareBackupExport`/`BackupExporter.buildShareIntent` (real `ACTION_SEND` + `FileProvider` chooser for the finished archive).

## Housekeeping

- [ ] Symlink-entry rejection in `BackupZipStructuralValidator` (needs Apache Commons Compress — new dependency, needs an ADR per AGENTS.md).
- [ ] If/when predictive back is enabled, migrate `AlarmActivity.onBackPressed()` to `OnBackPressedDispatcher`.
