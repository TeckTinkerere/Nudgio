# media/

Owns `MediaRepository`, the two-phase import journal (MR-09 "Import
atomicity") and `MediaPlayerActivity`'s Media3 integration (ADR-012).

Intentionally empty in this change; see `alarm/README.md` for why.
