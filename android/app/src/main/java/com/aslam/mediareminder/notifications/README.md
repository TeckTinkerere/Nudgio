# notifications/

Owns `NotificationCoordinator`, channel versioning (MR-06 "Notification
channels") and the notification action `PendingIntent` factories.

Intentionally empty in this change; see `alarm/README.md` for why.
