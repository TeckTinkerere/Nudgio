# MR-07: release is "minified only after native action tests pass."
# Project-specific ProGuard rules go here. React Native's own consumer rules
# (bundled with react-android) cover the framework; this file stays empty
# until a native class needs an explicit keep rule.
